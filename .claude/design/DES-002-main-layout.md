# UI 설계: 메인 레이아웃

> TODO: DES-002
> 작성일: 2026-01-15
> 담당: ui-designer

## 1. 와이어프레임

### Desktop (≥1024px) - 3단 구조
```
┌─────────────────────────────────────────────────────────────────────┐
│  Header (h: 64px)                                                   │
│  ┌────────┐  📄 filename.md    [☀️] [⚙️] [Download] [GitHub]        │
│  │  Logo  │                                                         │
│  └────────┘                                                         │
├────────┬──────────────────────────┬──────────────────────────────────┤
│        │                          │                                  │
│  TOC   │      Editor Pane         │      Preview Pane                │
│ Sidebar│   (CodeMirror)           │   (Markdown Render)              │
│        │                          │                                  │
│ (200px)│  ┌──────────────────┐    │  ┌──────────────────────────┐   │
│        │  │ EditorToolbar    │    │  │  # Preview Title         │   │
│  • H1  │  ├──────────────────┤    │  │                          │   │
│    H2  │  │                  │    │  │  Rendered content here   │   │
│    H2  │  │  Content...      │    │  │                          │   │
│  • H1  │  │                  │    │  │                          │   │
│        │  │                  │    │  │                          │   │
│        │  └──────────────────┘    │  └──────────────────────────┘   │
│ [접기] │                          │                                  │
│        │◀━━━━━ 드래그 리사이즈 ━━━▶│                                  │
└────────┴──────────────────────────┴──────────────────────────────────┘
```

### Tablet (768-1023px) - 2단 구조
```
┌─────────────────────────────────────────────────────────────┐
│  Header (h: 56px)                                           │
│  📄 filename.md    [☀️] [⚙️] [TOC] [Download]                │
├────────────────────────────┬────────────────────────────────┤
│                            │                                │
│      Editor Pane           │      Preview Pane              │
│   (CodeMirror)             │   (Markdown Render)            │
│                            │                                │
│  ┌──────────────────┐      │  ┌────────────────────────┐   │
│  │ EditorToolbar    │      │  │  # Preview Title       │   │
│  ├──────────────────┤      │  │                        │   │
│  │                  │      │  │  Rendered content...   │   │
│  │  Content...      │      │  │                        │   │
│  │                  │      │  │                        │   │
│  └──────────────────┘      │  └────────────────────────┘   │
│                            │                                │
│◀━━━━━ 드래그 리사이즈 ━━━━▶│                                │
└────────────────────────────┴────────────────────────────────┘

TOC는 헤더의 TOC 버튼 클릭 시 오버레이로 표시
```

### Mobile (<768px) - 탭 전환 방식
```
┌───────────────────────────────┐
│  Header (h: 48px)             │
│  📄 file.md  [☀️] [⚙️] [TOC]   │
├───────────────────────────────┤
│  [Editor] [Preview]  ← Tabs  │
├───────────────────────────────┤
│                               │
│                               │
│      Active Tab Content       │
│      (Editor or Preview)      │
│                               │
│  ┌─────────────────────────┐  │
│  │ EditorToolbar (Editor)  │  │
│  ├─────────────────────────┤  │
│  │                         │  │
│  │  Content...             │  │
│  │                         │  │
│  │                         │  │
│  └─────────────────────────┘  │
│                               │
└───────────────────────────────┘

탭 전환으로 Editor ↔ Preview 이동
TOC는 헤더의 TOC 버튼 클릭 시 전체 화면 오버레이
```

## 2. 컴포넌트 계층

```
App
├── Header
│   ├── Logo (Desktop만)
│   ├── FileNameDisplay
│   ├── ThemeToggle
│   ├── SettingsButton
│   ├── TOCToggleButton (Tablet/Mobile)
│   ├── DownloadButton
│   └── GitHubLink
├── MainLayout
│   ├── TOCSidebar (Desktop만 항상 표시)
│   │   ├── TOCHeader
│   │   ├── TOCList
│   │   │   └── TOCItem[]
│   │   └── CollapseButton
│   ├── SplitPane (Desktop/Tablet)
│   │   ├── EditorPane
│   │   │   ├── EditorToolbar
│   │   │   │   └── ToolbarButton[]
│   │   │   └── Editor (CodeMirror)
│   │   ├── ResizeHandle
│   │   └── PreviewPane
│   │       └── Preview (react-markdown)
│   └── TabView (Mobile)
│       ├── TabBar
│       │   ├── EditorTab
│       │   └── PreviewTab
│       └── TabContent
│           ├── EditorPane (activeTab === 'editor')
│           └── PreviewPane (activeTab === 'preview')
└── Modals
    ├── SettingsModal
    ├── TOCOverlay (Tablet/Mobile)
    └── FileDropZone (전체 화면 오버레이)
```

## 3. 컴포넌트 상세

| 컴포넌트 | 책임 | Props | State |
|----------|------|-------|-------|
| **Header** | 상단 네비게이션 바 | - | - |
| Logo | 로고 표시 (Desktop만) | - | - |
| FileNameDisplay | 현재 파일명 표시 | fileName, isDirty | - |
| ThemeToggle | 다크/라이트 모드 토글 | theme, onToggle | - |
| SettingsButton | 설정 모달 열기 | onOpenSettings | - |
| TOCToggleButton | TOC 오버레이 토글 (Tablet/Mobile) | isTOCOpen, onToggle | - |
| DownloadButton | 파일 다운로드 | onDownload | - |
| **MainLayout** | 메인 레이아웃 컨테이너 | screenSize | - |
| **TOCSidebar** | 목차 사이드바 | headings, activeId, isCollapsed, onCollapse | - |
| TOCHeader | 사이드바 헤더 | - | - |
| TOCList | 목차 리스트 | headings, activeId, onClickItem | - |
| TOCItem | 목차 항목 | heading, isActive, level, onClickItem | - |
| CollapseButton | 사이드바 접기 버튼 | isCollapsed, onToggle | - |
| **SplitPane** | 좌우 분할 패널 | leftSize, onResize | dragging |
| EditorPane | 에디터 영역 컨테이너 | - | - |
| ResizeHandle | 드래그 리사이즈 핸들 | onDragStart, onDrag, onDragEnd | - |
| PreviewPane | 프리뷰 영역 컨테이너 | - | - |
| **TabView** | 탭 전환 뷰 (Mobile) | activeTab, onTabChange | - |
| TabBar | 탭 바 | activeTab, onTabChange | - |
| EditorTab | 에디터 탭 버튼 | isActive, onClick | - |
| PreviewTab | 프리뷰 탭 버튼 | isActive, onClick | - |
| TabContent | 탭 콘텐츠 영역 | activeTab | - |

## 4. 상호작용

### 4.1 SplitPane 리사이즈 (Desktop/Tablet)

**동작:**
1. ResizeHandle 드래그 시작 (onMouseDown)
2. 마우스 이동 시 좌우 패널 크기 조정 (onMouseMove)
3. 드래그 종료 (onMouseUp)
4. 새 크기를 localStorage에 저장

**제약:**
- 최소 너비: 각 패널 300px
- 최대 너비: 전체 너비 - 300px
- 초기값: 50:50 비율

**시각적 피드백:**
- ResizeHandle 호버 시: 커서 `col-resize`
- 드래그 중: 반투명 오버레이 표시

### 4.2 TOC 네비게이션

**동작:**
1. TOCItem 클릭 시
2. 해당 헤딩으로 스크롤 (smooth behavior)
3. 클릭한 항목 하이라이트 (active state)

**스크롤 동기화:**
- 프리뷰 스크롤 시 현재 보고 있는 헤딩 자동 하이라이트
- 스크롤 감지: Intersection Observer 사용

### 4.3 TOC 사이드바 접기/펼치기 (Desktop)

**동작:**
1. CollapseButton 클릭
2. 사이드바 애니메이션 (width: 200px → 0px, 300ms ease-out)
3. 상태를 localStorage에 저장
4. 버튼 아이콘 변경: `◀` ↔ `▶`

### 4.4 TOC 오버레이 (Tablet/Mobile)

**동작:**
1. TOCToggleButton 클릭
2. 전체 화면 오버레이 표시 (애니메이션: fade-in)
3. TOCList 표시
4. 항목 클릭 또는 오버레이 외부 클릭 시 닫기

### 4.5 탭 전환 (Mobile)

**동작:**
1. EditorTab 또는 PreviewTab 클릭
2. activeTab 상태 변경
3. TabContent 전환 (애니메이션: slide)
4. 활성 탭 하이라이트

### 4.6 반응형 전환

**브레이크포인트 변경 시:**
- Desktop ↔ Tablet: SplitPane 유지, TOCSidebar 토글
- Tablet ↔ Mobile: SplitPane → TabView 전환
- 상태 유지: 에디터 내용, 스크롤 위치 유지

## 5. 반응형

| 화면 크기 | 레이아웃 | TOC 위치 | 패널 리사이즈 | 초기 비율 |
|-----------|----------|----------|---------------|-----------|
| **Desktop** (≥1024px) | 3단 (TOC + Editor + Preview) | 좌측 고정 사이드바 (200px) | 가능 (Editor ↔ Preview) | 50:50 |
| **Tablet** (768-1023px) | 2단 (Editor + Preview) | 헤더 버튼 → 오버레이 | 가능 (Editor ↔ Preview) | 50:50 |
| **Mobile** (<768px) | 탭 전환 (Editor 또는 Preview) | 헤더 버튼 → 전체화면 오버레이 | 불가 | - |

### 브레이크포인트

```css
/* Tailwind 기준 */
sm: 640px   /* Mobile 세로 → Mobile 가로 */
md: 768px   /* Mobile → Tablet */
lg: 1024px  /* Tablet → Desktop */
xl: 1280px  /* Desktop 큰 화면 */
```

### 반응형 동작

#### Desktop (≥1024px)
- TOC 사이드바 항상 표시 (접기/펼치기 가능)
- Editor와 Preview 좌우 분할
- ResizeHandle로 비율 조정 가능
- Header에 Logo 표시

#### Tablet (768-1023px)
- TOC 사이드바 숨김 → 헤더에 TOC 버튼 표시
- Editor와 Preview 좌우 분할
- ResizeHandle로 비율 조정 가능
- Header에 Logo 숨김

#### Mobile (<768px)
- 탭 전환 방식 (Editor ↔ Preview)
- SplitPane 비활성화
- TOC는 전체화면 오버레이로 표시
- Header 높이 축소 (48px)
- 버튼 크기 축소 (터치 친화적)

## 6. 접근성

### 키보드 네비게이션

| 키 | 동작 |
|----|------|
| `Tab` | 포커스 이동 (Header 버튼 → Editor → Preview → TOC) |
| `Shift + Tab` | 역방향 포커스 이동 |
| `Cmd/Ctrl + \` | TOC 사이드바 토글 (Desktop) |
| `Cmd/Ctrl + Shift + P` | 탭 전환 (Mobile) |
| `Enter` | TOC 항목 선택 시 해당 위치로 이동 |
| `Esc` | TOC 오버레이 닫기 (Tablet/Mobile) |

### 스크린리더

| 요소 | aria-label | role |
|------|------------|------|
| Header | "주 네비게이션" | banner |
| TOCSidebar | "목차" | navigation |
| TOCItem | "헤딩 제목으로 이동" | button |
| SplitPane | "에디터와 프리뷰 패널" | main |
| ResizeHandle | "패널 크기 조절" | separator |
| EditorPane | "마크다운 에디터" | region |
| PreviewPane | "마크다운 프리뷰" | region |
| TabBar | "에디터와 프리뷰 전환" | tablist |
| EditorTab | "에디터" | tab |
| PreviewTab | "프리뷰" | tab |

### 포커스 인디케이터

- 모든 인터랙티브 요소에 포커스 링 표시
- 색상: `focus-visible:ring-2 ring-primary ring-offset-2`
- 키보드 포커스만 표시 (마우스 클릭 시 표시 안 함)

### 색상 대비

- 모든 텍스트: WCAG AA 준수 (4.5:1 이상)
- 버튼/링크: WCAG AA 준수
- 포커스 링: 고대비 색상 사용

### 반응형 접근성

- Mobile: 터치 타겟 최소 44x44px
- Tablet/Desktop: 마우스 호버 상태 명확히 표시
- 모든 화면 크기에서 키보드 접근 가능

## 7. 레이아웃 전환 애니메이션

### TOC 사이드바 접기/펼치기 (Desktop)

```css
.toc-sidebar {
  width: 200px;
  transition: width 300ms ease-out;
}

.toc-sidebar.collapsed {
  width: 0;
  overflow: hidden;
}
```

### TOC 오버레이 (Tablet/Mobile)

```css
.toc-overlay {
  opacity: 0;
  transform: translateX(-100%);
  transition: opacity 200ms ease-out, transform 200ms ease-out;
}

.toc-overlay.open {
  opacity: 1;
  transform: translateX(0);
}
```

### 탭 전환 (Mobile)

```css
.tab-content {
  transform: translateX(0);
  transition: transform 200ms ease-out;
}

.tab-content[data-prev-tab] {
  transform: translateX(-100%);
}
```

### ResizeHandle 호버/드래그

```css
.resize-handle {
  transition: background-color 150ms ease-out;
}

.resize-handle:hover {
  background-color: var(--color-primary);
}

.resize-handle.dragging {
  background-color: var(--color-primary);
}
```

## 8. 상태 저장

### localStorage 키

| 키 | 값 | 설명 |
|----|-----|------|
| `md-preview-split-ratio` | `number (0-1)` | SplitPane 비율 |
| `md-preview-toc-collapsed` | `boolean` | TOC 사이드바 접힘 상태 |
| `md-preview-active-tab` | `'editor' \| 'preview'` | Mobile 활성 탭 |

### 초기값

- `split-ratio`: 0.5 (50:50)
- `toc-collapsed`: false (펼쳐짐)
- `active-tab`: 'editor' (에디터 탭)

## 9. 에러 상태

### 화면 크기 감지 실패

**증상:** 반응형 레이아웃이 올바르게 전환되지 않음

**처리:**
- 기본값: Desktop 레이아웃 사용
- window.innerWidth로 폴백

### 리사이즈 핸들 드래그 실패

**증상:** 드래그 중 마우스가 핸들을 벗어남

**처리:**
- document에 전역 이벤트 리스너 등록
- 마우스 업 시 항상 드래그 상태 해제

### TOC 스크롤 동기화 실패

**증상:** Intersection Observer 미지원 브라우저

**처리:**
- scroll 이벤트로 폴백
- 성능 최적화: throttle 300ms

## 10. 구현 우선순위

### P0 (필수)
- [x] Header 컴포넌트
- [x] Desktop 3단 레이아웃 (TOC + SplitPane)
- [x] SplitPane 리사이즈
- [x] TOC 사이드바 기본 기능
- [x] 반응형 브레이크포인트

### P1 (중요)
- [ ] TOC 스크롤 동기화
- [ ] TOC 접기/펼치기 애니메이션
- [ ] Mobile 탭 전환
- [ ] Tablet TOC 오버레이
- [ ] 상태 저장 (localStorage)

### P2 (선택)
- [ ] ResizeHandle 드래그 시 반투명 오버레이
- [ ] 탭 전환 애니메이션
- [ ] TOC 오버레이 애니메이션
- [ ] 키보드 단축키
- [ ] 접근성 최적화
